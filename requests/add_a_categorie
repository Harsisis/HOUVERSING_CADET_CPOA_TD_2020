
//title_cat is a variable we use to store the name of the future categorie
//visual_cat is a variable we use to store the visual of the future categorie

//else case is missing, it should be used in case title or/and visual is/are empty


String query = "INSERT INTO Categorie (titre, visuel) VALUES(";

if (title_cat <> "")
  query += "'" + title_cat + "',";  

if (visual_cat <> "")
  query += "'" + visual_cat + "'";

query += ");";
